#!/usr/bin/env bun

import path from "node:path"

const script = path.join(import.meta.dir, "..", "packages", "desktop", "scripts", "release.ts")
const proc = Bun.spawn(["bun", script, ...process.argv.slice(2)], { stdio: ["inherit", "inherit", "inherit"] })
process.exit(await proc.exited)
